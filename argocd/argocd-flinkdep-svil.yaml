apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: svil-fd-questionario-mifid-pf-pg
  namespace: openshift-gitops
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    name: 'in-cluster'
    namespace: myproject
  source:
    path: flink-instance/overlay/svil/
    repoURL: 'https://github.com/fdaluisio/apache-flink.git'
    targetRevision: HEAD
    kustomize: {}
      # components:
      #   - overlay/dev/
  syncPolicy: {}
